# test

```elixir
Mix.install([{:kino_sampler, path: "."}])
```

## Section

<!-- livebook:{"attrs":{},"chunks":null,"kind":"Elixir.KinoSampler","livebook_object":"smart_cell"} -->

```elixir
:ok
```

```elixir
dest_pid = KinoSampler.get_pid() |> Keyword.get(:pid) |> IO.inspect()
send(dest_pid, "finished")

# KinoSampler.finished(:finished)

# receive do
#   "finished" -> :ok
# end
# Kino.SmartCell.set_attribute(KinoSampler, my_attribute: "Hello, smart ass!")
# Kino.SmartCell.set_attribute("Hello, smart ass!")

# pid = self()
```
