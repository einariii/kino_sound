# test

```elixir
Mix.install([{:kino_sound, path: "."}])
```

## Section

<!-- livebook:{"attrs":{},"chunks":null,"kind":"Elixir.KinoSound","livebook_object":"smart_cell"} -->

```elixir
:ok
```

```elixir
dest_pid = KinoSound.get_pid() |> Keyword.get(:pid) |> IO.inspect()
send(dest_pid, "random")

# KinoSound.finished(:finished)

# receive do
#   "finished" -> :ok
# end
# Kino.SmartCell.set_attribute(KinoSound, my_attribute: "Hello, smart ass!")
# Kino.SmartCell.set_attribute("Hello, smart ass!")

# pid = self()
```
